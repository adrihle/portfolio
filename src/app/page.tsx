import { Playground } from "@/containers";
import { Layout } from "@/components";


const Home = async () => {
  return (
    <Layout>
      <Playground />
    </Layout>
  );
};

export default Home;

